{
  "name": "judetracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -k -n server,client -c cyan,magenta \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "tsx watch server/src/index.ts",
    "dev:client": "vite --config client/vite.config.ts",
    "typecheck": "tsc -p server/tsconfig.json && tsc -p client/tsconfig.json",
    "build": "npm run typecheck && npm run build:client",
    "build:client": "vite build --config client/vite.config.ts",
    "start": "tsx server/src/index.ts",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate --config server/drizzle.config.ts",
    "db:push": "drizzle-kit push --config server/drizzle.config.ts",
    "db:studio": "drizzle-kit studio --config server/drizzle.config.ts"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "drizzle-orm": "^0.44.3",
    "exifr": "^7.1.3",
    "express": "^5.1.0",
    "multer": "^2.0.1",
    "node-ical": "^0.20.1",
    "pg": "^8.16.3",
    "sharp": "^0.34.3",
    "tsx": "^4.20.3",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/multer": "^2.0.0",
    "@types/node": "^22.15.0",
    "@types/pg": "^8.15.5",
    "concurrently": "^9.2.0",
    "drizzle-kit": "^0.31.4",
    "typescript": "^5.8.3",
    "vite": "^7.0.5",
    "vitest": "^3.2.4"
  }
}
